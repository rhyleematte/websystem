
  # MindfulConnect Login Page

  This is a code bundle for MindfulConnect Login Page. The original project is available at https://www.figma.com/design/uYDuov6slTfoZpSmAiwTI7/MindfulConnect-Login-Page.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  